# 通し確認とカバレッジのループ

設計の正本は `C:\Users\yousu\OneDrive\デスクトップ\デスクトップ2\20.開発\図書館課題\02_設計\基本設計.md`。
優先の正本は同フォルダの `進捗と優先.md`。
ブランチは `feat/formal-e2e-coverage`。1周で未完了の切片を1つだけ製造し、検証してから止まる。

回数記録の関数、JSON、API、画面からのPOSTは既にある。作り直さない。QR画像、Tunnel、SQLiteは作らない。

## 1周の手順

1. このファイルの切片を上から見て、最初の未完了を1つ選ぶ。
2. その切片だけを実装する。次の切片には進まない。
3. 切片に書いた確認を実行し、結果をその切片のメモへ残す。
4. 確認が通ったら `[x]` にする。通らなければ `[ ]` のまま、失敗をメモへ残して止まる。
5. すべて `[x]` ならループを止める。

## 切片

- [x] 1. Playwright を入れ、`npm run test:e2e` で `/` を開く。初期は1階の地図。
  - 確認: 通しが1本通り、1階と印が見える。
  - メモ: `npm run test:e2e` で1本成功。見出し「館内案内」、1階、印11件、新刊の印が見える。
- [x] 2. 対象スポットをタップすると説明が出て、`GET /api/spot-taps` のそのIDが1増える。id 1 または id 8 では増えない。
  - 確認: 対象1件と入口1件を同じ通しで確かめる。
  - メモ: `npm run test:e2e` で2本成功。文芸書（id 2）は説明が出て tapCount が1増える。新刊（id 1）は説明は出るが回数は0のまま。
- [x] 3. 同じ対象スポットを閉じて再度開くと、回数がさらに1増える。幅は 390。
  - 確認: 開き直しで tapCount が2。印とシートが操作できる。
  - メモ: `npm run test:e2e` で3本成功。幅390で閉じたあと開き直すと id 2 が2増える。印とシートは操作できる。
- [x] 4. Vitest の対象コードに、行・文・関数・分岐の閾値 80% を書く。`spots.json` と設定とテスト自身は母数に入れない。
  - 確認: `npm test` が閾値つきで成功する。
  - メモ: `npm test` は17件成功。行 98.63%、文 98.7%、関数 96.66%、分岐 87.8%。閾値 80% を下回っていない。
