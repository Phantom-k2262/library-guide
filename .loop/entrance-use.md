# 入口で使うところまでのループ

設計の正本は `C:\Users\yousu\OneDrive\デスクトップ\デスクトップ2\20.開発\図書館課題\02_設計\基本設計.md`。
優先の正本は同フォルダの `進捗と優先.md`。
ブランチは `feat/entrance-qr`。1周で未完了の切片を1つだけ製造し、検証してから止まる。

地図、説明、回数の関数・JSON・APIは既にある。作り直さない。おすすめ本、イベント、スタンプラリー、管理画面、SQLiteは作らない。

## 1周の手順

1. このファイルの切片を上から見て、最初の未完了を1つ選ぶ。
2. その切片だけを実装する。次の切片には進まない。
3. 切片に書いた確認を実行し、結果をその切片のメモへ残す。
4. 確認が通ったら `[x]` にする。通らなければ `[ ]` のまま、失敗をメモへ残して止まる。
5. すべて `[x]` ならループを止める。

## 切片

- [x] 1. 入口2か所のQR。新刊・話題書コーナー用と予約受取ロッカー用。どちらも同じ案内のURL（`/`）を開く。
  - 確認: 2つのQRが同じパスを指す。ラベルだけが違う。
  - メモ: `npm test` は18件成功。2つのQRは path `/` と同一URL。ラベルは新刊と予約受取。画像は PNG の data URL。
- [x] 2. QRを印刷できる場所から出す。案内の操作を邪魔しない。
  - 確認: 2枚を画面または印刷用の経路で見られる。
  - メモ: `/print/qr` に2枚を出した。案内の `/` には置いていない。通し1本成功。
- [x] 3. Cloudflare Tunnel で HTTPS のURLを付ける。QRがそのURLを開く。
  - 確認: 館外の端末想定で、同じ案内が開く。
  - メモ: `GUIDE_PUBLIC_ORIGIN` があればQRはHTTPSの `/` を開く。`npm run tunnel` は cloudflared を `http://127.0.0.1:3000` に付ける。テスト20件成功。実トンネルは張っていない。
- [x] 4. 児童書（id 4）とおはなし会（id 18）を押し分けられるようにする。幅390。
  - 確認: 近い2件を別々にタップでき、それぞれの説明が開く。
  - メモ: 近い印は表示上3m以上離す。`npm test` 21件成功。幅390の通しで児童書とおはなし会を別々に開けた。
